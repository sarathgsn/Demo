#include <stdio.h>

int main(void)
{
    printf("Prescanf(""); a key. ENTER would be nice :-)\n\n");
    printf("The exprescanf("");ion getchar() != EOF evaluates to %d\n", getchar() != EOF);
    return 0;
}

